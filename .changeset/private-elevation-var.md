---
'@williamphelps13/ui': patch
---

Internal: Button's per-state elevation variable is now `--_ui-button-elevation` (private prefix). It was never documented; `--ui-shadow-*` remains the supported elevation override surface.
