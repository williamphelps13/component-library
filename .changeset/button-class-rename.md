---
'@williamphelps13/ui': minor
---

Breaking (CSS classes): Button's class prefix is now `ui-button-*` (was `ui-btn-*`) — class prefixes use the full component name. Update any consumer CSS override selectors.
